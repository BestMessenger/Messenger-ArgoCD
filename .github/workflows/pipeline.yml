name: Build, Test, and Deploy

on:
  push:
    branches:
      - main

jobs:

  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Run Number Test1
        run: echo ${{ github.run_number }}

      - name: Run Number Test2
        run: echo ${{ github.run_number }}



